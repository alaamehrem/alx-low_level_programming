#include "main.h"
/**
 * print_most_numbers - prints number other than 2 and 4
 */
void print_most_numbers(void)
{
	char i;

	for ( i = 48 ; i < 58 ; i++)
	{
		if ( i = 50 || i = 54)
			continue;
		else
			_putchar(i);
	}
	_putchar('\n');
}
